using System;

class Program
{
    static void Main()
    {
        Console.ForegroundColor = ConsoleColor.Red;
        Console.WriteLine("!!! WARNING !!!");
        Console.ResetColor();
        Console.WriteLine("This program looks suspicious!");
        Console.WriteLine("Would you like to run this malware? (Y/N)");

        ConsoleKeyInfo key = Console.ReadKey(true);
        if (key.Key == ConsoleKey.Y)
        {
            Console.ForegroundColor = ConsoleColor.DarkRed;
            Console.WriteLine("\nMalware is now running... Just kidding! This is a harmless prank.");
            Console.ResetColor();
        }
        else
        {
            Console.WriteLine("\nWise choice! Exiting safely.");
        }

        Console.WriteLine("Press any key to exit.");
        Console.ReadKey();
    }
}
